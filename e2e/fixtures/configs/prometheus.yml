global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  - job_name: "gunicorn-prometheus-exporter"
    static_configs:
      - targets: ["gunicorn-yaml-test:9094"]
    scrape_interval: 5s
    metrics_path: "/metrics"
