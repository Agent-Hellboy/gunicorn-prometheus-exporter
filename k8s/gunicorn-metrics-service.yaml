apiVersion: v1
kind: Service
metadata:
  name: gunicorn-metrics-service
  labels:
    app: gunicorn-app
    component: metrics
spec:
  type: ClusterIP
  ports:
    - port: 9091
      targetPort: 9091
      protocol: TCP
      name: metrics
  selector:
    app: gunicorn-app
